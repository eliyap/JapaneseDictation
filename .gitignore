node_modules/
package-lock.json
.DS_Store

# The synced database is created by the app through the GitHub API, not by a
# developer. Ignoring it locally avoids committing a stale copy by hand.
data/*.sqlite
