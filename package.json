{
  "name": "japanese-dictation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Listening dictation drill for Japanese, as a static site.",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1",
    "e2e": "node test/e2e.mjs",
    "audio": "node tools/generate-audio.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
